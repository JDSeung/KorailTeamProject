<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korail.batch.DAO.TrainPairingDAO">
	<select id="getStationInfo" resultType ="StationVO" parameterType="map">
		SELECT * FROM CITYACCOTTRAIN_TB
		<where>
			<choose>
				<when test="trainSerchType == 0">
					GYEONGBU != '0'					
				</when>
				<when test="trainSerchType == 1">
					GYEONGJEON != '0'						
				</when>
				<when test="trainSerchType == 2">
					HONAM != '0'						
				</when>
				<when test="trainSerchType == 3">
					JEOLLA != '0'						
				</when>
			</choose>
		</where>
	</select>
	
</mapper>
